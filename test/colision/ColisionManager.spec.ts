import { ColisionManager } from "../../src/colision/ColisionManager";

test("when no colision is added to handle, no callback should be called", () => {
    // const colisionManager = new ColisionManager();
});